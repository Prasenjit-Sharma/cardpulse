name = "cardpulse-api"
main = "worker.ts"
compatibility_date = "2025-09-01"

[vars]
GEMINI_MODEL = "gemini-3.5-flash-lite"
# Exact origins allowed to call the API. Add your own domain here if you use one.
ALLOWED_ORIGINS = "https://prasenjit-sharma.github.io"
# Per-account scan quota (migration 0002's consume_scan), called with each user's own token. Both values are public:
# they are the same ones the app ships with. Remove them to go back to the per-IP limit only.
SUPABASE_URL = "https://xqwslvteyhfmnxcnlpsg.supabase.co"
SUPABASE_PUBLISHABLE_KEY = "sb_publishable_xRiJzrH9yCIo_NwHN1jNDw_zWfTAiqf"

# Secret (never commit it):  npx wrangler secret put GEMINI_API_KEY
